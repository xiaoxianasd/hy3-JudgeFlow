# 结果与能力边界

## 2026-09-10：Hy3 65 道外部题完整评测

使用 `hy3`、Supervisor 五专家和每题 60 次 Hypothesis 生成预算，完成 60 道 MBPP / EvalPlus 与 5 道 TACO / CodeWars 函数级题。65/65 均完成，失败运行 0，待处理 0。原始机器结果为 `reports/hy3_external_20260909.json/jsonl/csv`，完整分析见 `reports/hy3_external_20260909_report.md`。

| 指标 | 结果 |
|---|---:|
| 最终答案正确率 | 57/65（87.7%） |
| 过程判定覆盖率 | 58/65（89.2%） |
| 已判定过程中的成立率 | 41/58（70.7%） |
| 过程不成立 | 17 |
| 过程弃判 | 7 |
| 答案正确但过程不成立 | 9 |

按当前题库难度标签，easy / medium / hard 的最终答案正确率为 92.9% / 96.2% / 76.0%，过程成立率（仅统计明确判定）为 75.0% / 85.7% / 56.0%。下降信号出现在 hard 层，但外部题难度仍采用 AST 启发式；`data/annotations/difficulty_calibration_external_20260909.json` 是待人工抽查的五维量表草案，尚未回写本轮记录，因此不能把该信号表述为已经确认的能力临界点。

本轮 65 条属于自然生成样本，与下方受控构造样本分开统计。自动评估器为 17/17 个负面过程给出了首错位置，其中 9 条为“答案正确但过程不成立”；这些是待人工抽检的自动标记结果。外部样本尚未完成独立人工复核，所以不据此报告定位准确率、漏检数或误报数。相关有效性指标仍以受控样本和已完成盲审的 6 道种子题为准。

## 2026-09-08：Hy4 preview 正确代码、错误过程专项验证

新增12条受控配对样本：6条 `correct_code_wrong_proof` 只把标准证明替换成错误证明，6条 `condition_overgeneralization` 只把边界步骤替换成错误条件推广。另取6条未修改的标准过程作为误报对照。18条均使用参考代码，固定测试、属性测试以及错误样本附带的反例输入全部通过，因此最终答案金标均为正确。

使用 `hy4-preview`、Single审查、`reasoning_effort=low` 运行。多次尝试按 `sample_id` 去重，优先选择明确结论；若多次明确结论冲突则拒绝自动合并。完整机器结果为 `reports/hy4_preview_process_validation_20260908.json/jsonl/csv`：

| 受控类型 | 样本 | 检出 | 漏检 | 误报 | 真阴性 | 弃判 | 首错精确命中 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 正确代码＋错误证明 | 6 | 3 | 0 | 0 | 0 | 3 | 3/3 |
| 错误条件推广、代码仍正确 | 6 | 2 | 0 | 0 | 0 | 4 | 2/2 |
| 标准正确过程对照 | 6 | 0 | 0 | 0 | 4 | 2 | 不适用 |
| 合计 | 18 | 5 | 0 | 0 | 4 | 9 | 5/5 |

错误过程的全样本检出为5/12（41.67%），覆盖范围内为5/5；后者必须与41.67%覆盖率同时报告，不能把7条弃判从能力结论中隐藏。5个检出全部精确定位到金标步骤，错误类型分布为 `concept_error` 4条、`condition_omission` 1条。正确过程对照覆盖4/6，误报为0/4；另2条因HTTP 429弃判。

弃判主要来自TokenHub高负载导致的HTTP 429、读超时、缺少最终内容或结构化输出失败。客户端已增加TokenHub JSON模式、推理强度配置、最终内容/推理内容区分、逐样本冷却及精确重试筛选。腾讯云文档也提示 `hy4-preview` 当前资源负载较高、高峰可能触发限频。首次Hy3运行因额度HTTP 402而产生的历史弃判结果仍保存在 `reports/controlled_process_20260908.json`，不与本次Hy4结果合并。

自然生成样本继续使用2026-09-07的6条真实Hy3结果及独立人工记录，单独汇总在 `reports/natural_process_20260908.json`：

| 自然生成范围 | 样本 | 检出 | 漏检 | 误报 | 弃判 |
|---|---:|---:|---:|---:|---:|
| 全部人工复核样本 | 6 | 1 | 0 | 1 | 0 |
| 金标最终答案正确 | 5 | 0 | 0 | 1 | 0 |

自然样本中的检出是代码语法错误，误报是旧沙盒导入契约造成的BFS误判；没有自然生成的“答案正确、过程错误”样本。因此，当前自然结果仍不能验证该能力。受控构造与自然生成结果没有合并计算。

统计定义：检出=金标过程错误且预测错误；漏检=金标过程错误但预测成立；误报=金标过程成立但预测错误；弃判=有过程金标但预测为未知或缺失。未完成人工标注的自然样本单列为未标注，不计入弃判。

## 2026-09-07：真实 Hy3 分层试运行

使用 `hy3`、Supervisor 五专家复核和每题60次 Hypothesis 预算，对6道量表校准后的种子题按 easy / medium / hard 各抽2道。完整分析和典型案例见 `reports/hy3_seed_stratified_20260907_report.md`，原始记录见同名前缀的 JSON/JSONL/CSV，人工复核汇总见 `reports/hy3_seed_stratified_20260907_validity.json`。

| 口径 | easy | medium | hard | 总体 |
|---|---:|---:|---:|---:|
| 自动评估 | 2/2 | 1/2 | 1/2 | 4/6（66.67%） |
| 人工复核 | 2/2 | 2/2 | 1/2 | 5/6（83.33%） |

人工复核覆盖6/6。人工认定的1个错误答案被正确检出并定位到第6步，问题检出率和首错 exact-match 定位准确率均为100%（1/1）。5个答案正确且过程成立的样本中有1个评估器误报，正确过程误报率为20%（1/5）；该历史误报来自生成提示与沙盒导入策略不一致，导致正确 BFS 实现被拒绝。当前版本已让生成提示和执行器共用安全导入白名单，并在沙盒运行器的本地后端重新验证该样例通过；Docker 端到端重放仍需在装有 Docker 的环境执行。

人工复核后的点估计在 easy、medium 均为100%，hard 为50%。每层只有2题且置信区间大幅重叠；本轮只能说明 hard 出现待验证的下降信号，不能确认能力临界点。

难度使用五维人工量表：算法识别、状态/不变量、证明负担、边界陷阱、复杂度约束各0-3分；0-6为 easy、7-10为 medium、11-15为 hard。逐题证据见 `data/annotations/difficulty_calibration_seed_20260907.json`。

## 2026-09-07：修复后的离线构造集（历史结果）

本报告基于当前代码重新执行 `benchmark --source fixtures --hypothesis-examples 20`，对应 `reports/fixtures_benchmark.json/jsonl/csv`。这是执行与弃判行为验收，不是 Hy3 模型能力结果。旧版依赖关键词规则得到的“100% 首错定位、0% 误报”已撤下，不能作为修复后评审器的验证数据。

| 指标 | 当前结果 |
|---|---:|
| 去重并加入对抗变换后的样本 | 28 |
| 测试通过 | 22 / 28 |
| 检出实现失败 | 6 / 28 |
| 未启用语义评审，过程证据不足 | 22 |
| 已确认过程/实现有问题 | 6 |
| 过程评估覆盖率 | 21.43% |
| 错误答案上的问题检出率 | 100.00% |
| 精确匹配构造金标的首错定位率 | 0.00% |
| 正确过程上的误报率 | 未评估：没有明确语义结论 |

离线执行能证明 6 个候选实现有问题，但不能证明其最早的文字推理错误在哪里。当前实现证据指向隐式代码步骤，因此未匹配构造金标中的更早推理错误。22 个测试通过样本保留过程未知，其中既包含正确推导，也包含关键词堆砌、错误推导配正确代码等样本；不能把未知记作正确或错误。

统计中的 `process_accuracy=0` 仅表示已明确判定的 6 个样本均被判有问题，不能解释为全部 28 个过程都错了。覆盖率及未知数必须一同展示。

## 覆盖与回归案例

- 6 道种子题；原 24 条记录去重后为 16 条，再增加 6 条关键词堆砌/循环论证、6 条正确否定错误示例，共 28 条。
- 正确硬币兑换解答中否定“反复选择当前最大硬币”，不再被词汇规则直接判为步骤 2 错误。明确完整的语义正面复核可确认其成立。
- 用关键词代替推导，离线模式不再确认过程成立；其语义错误检出能力需启用 Hy3 后测量。
- 参考程序抛异常、候选返回 `None` 时，返回 `ReferenceOracleError`；合法参考程序返回 `None` 仍可验证。
- 构造集随附的 `_human_audit.jsonl` 只保存最终答案正确样本的22条受控金标，且明确标记 `independent_human_audit=false`；它不是独立人工复核结果。真实 Hy3 试运行的6条盲审记录已由复核者完成并另行汇总。

## 真实结果复现

具体步骤见 [评估与人工复核流程](EVALUATION_WORKFLOW.md)。

```bash
tracejudge benchmark --source fixtures --hy3-review --output reports/fixtures_hy3_review.json
tracejudge benchmark --source hy3 --tier seed --per-difficulty 2 --output reports/hy3_seed.json
tracejudge audit-export --benchmark reports/hy3_seed.json --output reports/hy3_seed_audit.jsonl
# 由实际复核者填写后再汇总
tracejudge audit-summary --benchmark reports/hy3_seed.json --annotations reports/hy3_seed_audit.jsonl --output reports/hy3_seed_validity.json
```

上述命令会重新调用 Hy3，受模型随机性影响，不保证与本轮结果逐项相同。当前 6 道种子题已经完成量表校准；65 道外部题的五维量表仍是待人工抽查草案，当前机器结果沿用 AST 启发式难度，不能直接用于能力临界点结论。
