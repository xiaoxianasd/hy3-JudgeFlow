# Raw data

此目录默认不提交上游大数据。请阅读 `data/manifests/sources.json`，自行下载并确认许可后放到各自子目录：

```text
data/raw/taco/
data/raw/livecodebench/
data/raw/cruxeval/
data/raw/processbench/
data/raw/prmbench/
```

当前仓库自带的 `data/problems.json` 是 6 道原创参数化种子题，可离线完成全链路验收。
